{
  "scripts": {
    "build": "node ./node_modules/gulp/bin/gulp.js",
    "watch": "npm run build && node ./node_modules/gulp/bin/gulp.js watch",
    "production": "node ./node_modules/gulp/bin/gulp.js --production",
    "deploy": "git checkout -b deploy && npm run production && git add public/css/ public/js/ -f && git commit -m \"build\" && eb deploy && git checkout master && git branch -D deploy"
  },
  "private": true,
  "devDependencies": {
    "gulp": "^3.8.8",
    "laravel-elixir": "*"
  }
}
